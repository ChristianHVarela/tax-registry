version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"  
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://taxregistry-mysql:3306/taxregistry 
      SPRING_DATASOURCE_USERNAME: root  
      SPRING_DATASOURCE_PASSWORD: Root123!  
      TOKEN_SECRET: y7H8@wP9u#T$R0pL1kV6b!Qz4E5nM2xN9sF3cZ7gU@t
    depends_on:
      - taxregistry-mysql

  taxregistry-mysql:
    image: mysql:8.0  
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Root123!
      MYSQL_DATABASE: taxregistry  
      MYSQL_USER: root  
      MYSQL_PASSWORD: Root123!  
    ports:
      - "3307:3306" 
    volumes:
      - mysql-data:/var/lib/mysql 

volumes:
  mysql-data:  
